<!--pages/landing/landing.wxml-->
<loading hidden="{{loadingHidden}}">
      page loading...
</loading>
<view class="pageContainer">
  <view class="header">
    <view class="card-banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1415369629372-26f2fe60c467?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=387&q=80);">
      <view class="banner-description">
        <view class="h1">Adoption Day</view>
        <view class="h2">Aug 13</view>
        <!-- <button class="btn-white" href="#">see events</button> -->
      </view>
    </view>
  </view>

  <view class="card-title">
    <text class="h1">fluffy friends waiting for adoption</text>
  </view>

  <view class="description">
    <text class="h1">⇦ ⇨</text>
  </view>

  <view class="card-title">
    <text class="h1">all cats</text>
  </view>
  <scroll-view class="scroll-box" scroll-x scroll-with-animation
    scroll-into-view="{{toView}}"
    bindtouchstart="touchStart"
    bindtouchend="touchEnd">
    <view wx:for="{{cats}}" wx:for-item="cat" class="card-box">
      <view class="card">
        <text>{{cat.name}}</text>
      </view> 
    </view>
  </scroll-view>

  <!-- <scroll-view class="scroll-box" scroll-x scroll-with-animation
    scroll-into-view="{{toView}}"
    bindtouchstart="touchStart"
    bindtouchend="touchEnd">
      <view wx:for="{{pets}}" wx:for-item="pet" class="card-box" data-id="{{pet.id}}" bindtap="goToPet">
        <view class="card"> -->
          <!-- <image class="card-image" mode="aspectFill" src="{{pet.image_url}}"></image>
          <view class="pet-description"><text class="h1">{{pet.name}}</text></view>
          <view class="pet-description"><text class="h2">{{pet.district}}</text></view>
          <view class="pet-description"><text class="h2">{{pet.description}}</text></view> -->
        <!-- </view>
      </view>
  </scroll-view> -->

  <navigator open-type="switchTab" url="/pages/pets/index">
    <button class="weui-btn-blue">more pets</button>
  </navigator>
  
</view>
