<!--pages/users/profile.wxml-->
<text>pages/users/profile.wxml</text>

<!-- if logged in  -->
<view wx:if="{{!pets}}">no pets</view>
<view style="background-color: lightblue;">
  <view class="card" wx:for="{{pets}}">
    test{{item.name}}
  </view>
</view>
